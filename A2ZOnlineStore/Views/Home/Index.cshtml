@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered Products of the week </h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var Product in Model.PreferredProducts)
    {
        @Html.Partial("ProductsSummary", Product)
    }
</div>